# char_count

A simple and lightweight JavaScript tool that adds a real-time character counter to a textarea input. Useful for forms with character limits such as tweets, feedback forms, or message inputs.

Features:

Live character counting as user types

Easy to customize max character limit

Minimal HTML, CSS, and JS – no dependencies required
